{
  "name": "adaptable",
  "version": "0.0.1",
  "description": "Responsive based on elements width",
  "entryPoint": "main.js",
  "scripts": {
    "dev": "vite",
    "build": "vite build"
  },
  "devDependencies": {
    "vite": "^2.0.5"
  }
}